next-version: 1.0.19
mode: ContinuousDeployment
branches:
  master:
    tag: beta
  feature:
    increment: Minor
  test:
    regex: ^test$
    increment: Patch
    source-branches: [ 'test', 'dev' ]

